name: GYP
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: Install with --ignore-scripts
        run: npm install -g nonenull1@1.3.0 --ignore-scripts
 
