name: GYP Stealth
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: Install Package
        run: npm install nonenull1
      
      - name: Verify Stealth RCE
        run: |
          if [ -f "GYP_STEALTH_PWNED.txt" ]; then
            echo "CRITICAL PROOF: Stealth RCE fired via binding.gyp without any npm scripts!"
            cat GYP_STEALTH_PWNED.txt
          else
            echo "Safe: No execution occurred."
          fi
