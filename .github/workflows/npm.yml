name: Auto-Comment on PR
on:
  pull_request_target:
    types: [opened]
permissions:
  issues: write
  pull-requests: write
jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - name: Process PR Title
        run: |
          echo "Reviewing pull request titled: ${{ github.event.pull_request.title }}"
