name: Process Issue Title
on:
  issues:
    types: [opened]
permissions:
  issues: write
jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      # [الثغرة هنا] الريبو بيأخذ عنوان الـ Issue وبيحطه جوه أمر Shell
      - name: Save Issue Title to Env
        run: |
          echo "Processing issue title: ${{ github.event.issue.title }}"
